[project]
name = "mcp-websearch"
version = "0.1.0"
description = "DuckDuckGo web search MCP server over streamable-HTTP"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.27.2",
    "ddgs>=9.0",
]

[tool.uv]
package = false
