# Bridged network (no host_network): agent reaches this by internal DNS name
# `<repo>-mcp_websearch:8786`. No `ports:` mapping — internal-only, never on the LAN.
name: MCP Websearch
version: "0.2.1"
slug: mcp_websearch
description: Web search (DuckDuckGo) + page reader (trafilatura → markdown w/ links & images) for the reSpeaker agent
url: https://github.com/mosa-riel/mcp-websearch
arch:
  - aarch64
  - amd64
init: true
boot: auto
startup: services
